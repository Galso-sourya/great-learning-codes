import App from './components/App';
import { BrowserRouter } from 'react-router-dom';
/*we have to also run the below command line---
npm i react-router-dom @types/react-router @types/react-router-dom --force
*/
export default(
    <BrowserRouter>
 <App/>   
    </BrowserRouter>

);
