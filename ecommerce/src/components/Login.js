const Login=()=>{
    return(
        <div>
            Login Component
        </div>
    )
}
export {Login}//we are using this line because we are going to use Login component in other pages as well.